# Example: Convert a decimal number to hexadecimal
decimal_number = 79

# Using the hex() function and removing the "0x" prefix
hex_representation = hex(decimal_number)[2:]

print(f"The hexadecimal representation of {decimal_number} is: {hex_representation}")
